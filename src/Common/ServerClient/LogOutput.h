#ifndef SC_LOGOUTPUT_H_
#define SC_LOGOUTPUT_H_

typedef enum tagELogOutput_Commands{
  LogOutput_Command_Clear = 0x01,     // clear the log file (no more data should arrive)
} ELogOutput_Commands;

#endif