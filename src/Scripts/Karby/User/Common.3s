//====================================================================
// pomocne funkce

// minimum
int min(int a, int b)
{
  if (a < b)
    return a;
  else
    return b;
}

// maximum
int max(int a, int b)
{
  if (a < b)
    return b;
  else
    return a;
}
